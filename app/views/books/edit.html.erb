<h1>Editing Book</h1>

<form>
<%= render 'form', book: @book %>
</form>

<%= link_to "Show", book_path(@book.id) %> |  <!--''を""に修正、@bookをbook_path(@book.id)に修正-->
<%= link_to "Back", books_path %>  <!--''を""に修正-->

